{
    "version": 2,
    "installCommand": "npm install --force",
    "buildCommand": "npm run build",
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node",
            "config": {
                "postinstall": {
                    "command": "npm install --force"
                }
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
