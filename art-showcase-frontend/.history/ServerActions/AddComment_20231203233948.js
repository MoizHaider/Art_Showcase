"use server"

export default async function AddComment() {
    const graphqlQuery = {
        query: `mutation addCommnentQuery $postId: ID, $text: String){
                addComment(postId: )
        
        
        }`
    }
  
  
    return ''
}
